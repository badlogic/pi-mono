/**
 * Bun path implementation — same as Node.js.
 */
export {
	basename,
	delimiter,
	dirname,
	isAbsolute,
	join,
	relative,
	resolve,
	sep,
} from "node:path";

export { default } from "node:path";
