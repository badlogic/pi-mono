export const isNode = true;
export const isBrowser = false;

/** Never true when resolved via the Node.js export condition */
export const isBun = false;

/** Never true when resolved via the Node.js export condition */
export const isBunBinary = false;
