/**
 * Node.js path implementation — thin re-export of built-in path module.
 */
export {
	basename,
	delimiter,
	dirname,
	isAbsolute,
	join,
	relative,
	resolve,
	sep,
} from "node:path";

export { default } from "node:path";
