/**
 * Node.js path implementation — thin re-export of built-in path module.
 */
export {
	basename,
	default,
	delimiter,
	dirname,
	isAbsolute,
	join,
	relative,
	resolve,
	sep,
} from "node:path";
