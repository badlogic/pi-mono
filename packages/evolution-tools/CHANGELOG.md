## [Unreleased]

### Added

- Initial `evolution` extension package with bounded `setup`, `targets`, `init_dataset`, `run`, and `status` actions for Pi-native skill evolution.
- Added bundled `skill-evolution` skill plus a trimmed Python helper that adapts the Hermes self-evolution Phase 1 workflow to Pi skill roots and `.pi/evolution` output paths.
